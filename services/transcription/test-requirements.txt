# Lightweight deps for the real ASR regression test (no torch/pyannote needed).
pytest==8.3.3
faster-whisper==1.0.3
requests>=2.31  # faster-whisper 1.0.3 imports it but newer huggingface-hub no longer pulls it in


